import type { GSK_VARIABLE_NUMBER } from "@/library/types/variables";

export const evaluateExpression = (variables: GSK_VARIABLE_NUMBER[]) => {
  console.log("Evaluating expressions for variables:", variables);
};
